sonar.projectKey=ConMeoKeuSao-MobileDev251_barberqueue-class-exercise
sonar.organization=conmeokeusao-mobiledev251

# Source and test configuration
sonar.sources=app,components,hooks,utils
sonar.tests=__tests__
sonar.test.inclusions=**/*.test.ts,**/*.test.tsx

# Exclusions
sonar.exclusions=**/node_modules/**,**/coverage/**,**/*.d.ts,**/scripts/**,**/__mocks__/**

# Coverage report path (LCOV format)
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# TypeScript/JavaScript settings
sonar.typescript.tsconfigPath=tsconfig.json

# Project info
sonar.projectName=BarberQueue
sonar.projectVersion=1.0.0


# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8